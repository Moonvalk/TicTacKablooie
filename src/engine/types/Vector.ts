/**
 * Represents a two point vector position.
 */
export type Vector2 = {
    x: number;
    y: number;
};

/**
 * Represents a three point vector position.
 */
export type Vector3 = {
    x: number;
    y: number;
    z: number;
}