/**
 * Provides an index for each available type of renderable object.
 */
export enum MVRenderableType {
    /**
     * An image element.
     */
    Image,

    /**
     * A text element.
     */
    Text
}